# Core PyTorch dependencies (required for both training and inference)
# Note: PyTorch must be installed separately with CPU-only index:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# These lines are commented out to avoid installation conflicts
# torch
# torchaudio  
# torchvision

# Data processing and scientific computing (required for both training and inference)
# Use more conservative versions to avoid compilation issues on shared hosting
numpy>=1.19.0,<1.25.0
scipy>=1.7.0,<1.10.0

# Progress bars (required for both training and inference)
tqdm>=4.60.0

# System monitoring (required for both training and inference)
psutil>=5.8.0

# =============================
# UI / Inference Dependencies
# =============================
# Only needed for interactive play, web API, or CLI tools
flask>=2.0.0,<3.0.0
flask-cors>=3.0.0,<5.0.0
requests>=2.25.0,<3.0.0

# =============================
# Training-Only Dependencies
# =============================
# Optional: wandb for experiment tracking
# wandb>=0.21.0

# =============================
# Development / Testing (Optional)
# =============================
# Uncomment these for development environment
# pytest>=8.4.0
# pytest-cov>=6.2.0
# black>=25.1.0
# flake8>=7.3.0
# mypy>=1.16.0
# matplotlib>=3.10.0
# seaborn>=0.13.0
# jupyter>=1.1.0
# notebook>=7.4.0
# ipykernel>=6.29.0
