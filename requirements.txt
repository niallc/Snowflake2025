# Core PyTorch dependencies (required for both training and inference)
# NOTE: PyTorch is included below but you may want to install from official index for better performance:
# For CPU: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# For CUDA: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For MPS (Apple Silicon): pip install torch torchvision torchaudio (default PyPI is fine)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Data processing and scientific computing (required for both training and inference)
numpy>=1.21.0,<2.0.0

# Progress bars (required for both training and inference)
tqdm>=4.60.0

# System monitoring (required for both training and inference)
psutil>=5.8.0

# =============================
# UI / Inference Dependencies
# =============================
# Only needed for interactive play, web API, or CLI tools
flask>=2.0.0,<4.0.0
flask-cors>=6.0.0
requests>=2.25.0,<3.0.0

# =============================
# Training-Only Dependencies
# =============================
# Optional: wandb for experiment tracking
# wandb>=0.21.0

# =============================
# Development / Testing (Optional)
# =============================
# Uncomment these for development environment
# pytest>=8.4.0
# pytest-cov>=6.2.0
# black>=25.1.0
# flake8>=7.3.0
# mypy>=1.16.0
# matplotlib>=3.10.0
# seaborn>=0.13.0
# jupyter>=1.1.0
# notebook>=7.4.0
# ipykernel>=6.29.0
